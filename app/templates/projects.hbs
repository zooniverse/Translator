<div class="projects container">
  <h1 class="row">Translate Projects:</h1>
  
  <div class="list row">
    {{#each model}}
      <div {{bind-attr class=":project isAccessible::disabled"}}>
        {{link-to display_name "project" name classNames="button"}}
      </div>
    {{/each}}
  </div>
</div>
